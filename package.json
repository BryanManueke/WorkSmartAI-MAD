{
  "name": "worksmartai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm start --prefix frontend",
    "android": "npm run android --prefix frontend",
    "ios": "npm run ios --prefix frontend",
    "web": "npm run web --prefix frontend"
  },
  "dependencies": {
    "convex": "^1.35.1"
  }
}
